# ===========================================
# Jira API Configuration
# ===========================================

# Required Settings
# ------------------------------------------
# Your Jira instance hostname (without https://)
JIRA_HOST=your-instance.atlassian.net

# Your Jira account email
JIRA_EMAIL=your-email@example.com

# API token from https://id.atlassian.com/manage-profile/security/api-tokens for cloud
# or your plain Jira password for server instances
JIRA_API_TOKEN=your-api-token

# Optional Settings
# ------------------------------------------
# Jira API version to use (default: "3")
JIRA_API_VERSION=3

# Comma-separated list of fields to retrieve when fetching issues
# Include any custom fields you want to access by default
JIRA_CUSTOM_FIELDS=summary,description,status,priority,assignee,issuetype,parent,subtasks,labels,components,created,updated,reporter

# ===========================================
# Logging Configuration
# ===========================================
# Possible values: debug, info, warn, error
# debug: Most verbose, shows all details
# info: Default, shows general information
# warn: Shows only warnings and errors
# error: Shows only errors
LOG_LEVEL=info

# ===========================================
# Advanced Settings
# ===========================================
# Set to false if your Jira instance uses a self-signed certificate (default: true)
# JIRA_STRICT_SSL=true

# Maximum number of retries for API calls (default: 3)
# JIRA_MAX_RETRIES=3

# Timeout in milliseconds for API calls (default: 10000)
# JIRA_TIMEOUT=10000 